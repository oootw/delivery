---
- name: Выполнить rollout core-релиза на data-plane хосты
  hosts: dataplane
  gather_facts: true
  become: true
  serial: "{{ rollout_serial | default(1) }}"
  any_errors_fatal: false
  max_fail_percentage: 100
  roles:
    - deploy
