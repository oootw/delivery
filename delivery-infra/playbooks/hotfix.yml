---
- name: Экстренный hotfix rollout на production
  hosts: production
  gather_facts: true
  become: true
  serial: "{{ hotfix_serial | default(rollout_serial | default(1)) }}"
  roles:
    - deploy
